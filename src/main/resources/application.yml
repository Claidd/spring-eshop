spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
#    generate-ddl: true
#    dialect: org.hibernate.dialect.PostgreSQLDialect
#    database: POSTGRESQL


  datasource:

    url: jdbc:postgresql://localhost:5432/shop
    username: postgres
    password: 5453
    driverClassName: org.postgresql.Driver

  main:
    allow-circular-references: true

  flyway:
    baseline-on-migrate: true

server:
  hostname: localhost
  port: ${PORT:8080}
mail:
  server:
    host: smtp.gmail.com
    port: 587
    username: ivanhunt138@gmail.com
    password: hwaafjdasvubhwmg
